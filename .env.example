# Backend server origin without /api suffix (e.g. http://localhost:8080)
VITE_API_BASE_URL=

# Set to 1 to bypass real Google OAuth and use a fixed mock user.
VITE_MOCK_AUTH=

# Optional dev-only session token injected by Vite proxy as:
# Cookie: __Host-session=<value>
DEV_PROXY_SESSION_COOKIE=
